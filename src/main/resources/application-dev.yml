spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cv_platform
    username: postgres
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true

app:
  admin:
    password: ${ADMIN_PASSWORD}

jwt:
  secret: ${JWT_SECRET}

logging:
  level:
    com.nuria: DEBUG
    org.springframework.security: DEBUG